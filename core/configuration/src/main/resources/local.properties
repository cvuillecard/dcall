# SSL Configuration

keystore.path=${project.extern.resources.directory}/${keystore.path}
truststore.path=${project.extern.resources.directory}/${truststore.path}

keystore.pwd=${keystore.pwd}
truststore.pwd=${truststore.pwd}

# DEFAULT CLUSTER
cluster.host.ip=${cluster.host.ip}
cluster.host.port=${cluster.host.port}

# RUNTIME
extern.runtime.conf.name=${extern.runtime.conf.name}
extern.runtime.conf.path=${extern.runtime.conf.path}
system.git.repository=${project.extern.resources.directory}