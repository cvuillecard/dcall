# SSL Configuration

keystore.path=${project.extern.resources.directory}/${keystore.path}
truststore.path=${project.extern.resources.directory}/${truststore.path}

keystore.pwd=${keystore.pwd}
truststore.pwd=${truststore.pwd}
